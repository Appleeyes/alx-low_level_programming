#include "main.h"

/**
 * add - this is a function that adds two integers and returns the result.
 * @one: input number
 * @two: another input number
 * Return: result of adding 1 and 2
 */

int add(int one, int two)
{
	return (one + two);
}

